# Scripts

Prefer the package CLI:

```bash
baci build-composite --config configs/default.yaml
```

If you need to regenerate the individual component NetCDF files, use the
upstream `aci.components` package and save the outputs listed in
`configs/default.yaml`.
