# Notebooks

Keep exploratory notebooks here for transparency and provenance.

Recommended convention:

```text
notebooks/index_VF.ipynb
notebooks/archive/
```

The cleaned, reviewable implementation should live in `src/baci_climate_index`
and be run through the `baci` command-line interface.
